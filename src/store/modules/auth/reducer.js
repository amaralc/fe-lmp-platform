/* --------------------------------- CONTENT ---------------------------------*/
const INITIAL_STATE = {};

/* --------------------------------- EXPORTS ---------------------------------*/
export default function auth(state = INITIAL_STATE, action) {
  switch ((action, type)) {
    default:
      return state;
  }
}
